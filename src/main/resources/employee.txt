1.公共字段填充
@TableFiled  指定自动填充的字段  这些字段多为公共字段且内容较为模板化。然后新建一个类实现自 MateObjectHandler接口
从而在这个类中对与这些各个字段进行赋值操作，然后以后只要是涉及到了对于这些字段的操作，都是不同在各自的逻辑中去
写为这些字段赋值的代码的，这样子提高了代码的复用性。并且从前端所发送过来的数据也都会到这个类当中，称为元数据，
在这个类最终为标上注解的属性进行赋值。
2.file.transferTo();//将临时文件转存到指定的位置